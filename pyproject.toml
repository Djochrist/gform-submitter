[project]
name = "gform-submitter"
version = "1.0.0"
description = "Google Forms bulk submitter — standalone, fully configurable"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "rich>=13",
]

